# 抛币动画版 · 更新说明

这一版把"点一下直接出卦"升级成了你要的：**亲手抛掷六次铜钱成卦**。

## 改了什么
- 前端 `public/index.html`：加入三枚乾隆铜钱的抛掷动画（向上抛 + 空中翻转 + 落地轻弹），
  点"起卦"显示掷币台 → 亲手点"抛掷"六次，每次成一爻，自下而上累积 → 六爻齐自动出卦象与解读。
- 后端 `server.js`：新增 `resolveHexagram()`，接收前端摇好的六爻来查卦（不再后端随机）；
  接口 `/api/divine` 现在接受 `{ question, yaos }`，yaos 是六爻数组（每项 {value:6/7/8/9}）。
- 新增 `public/assets/coin-front.png` 和 `coin-back.png`：你那两枚真实乾隆铜钱（已抠图、透明、正圆）。

## 怎么用（和以前一样）
1. 把整个文件夹覆盖到你电脑的项目位置（保留你的 node_modules，不用重装）。
2. PowerShell 里：`Ctrl+C` 停掉 → `node server.js` 重启（密钥那行若窗口关过要重设）。
3. 浏览器刷新 http://localhost:3000 ，按 Ctrl+F5 强制刷新（让新图片和代码生效）。

## 体验流程
心中所问 → 点"起卦" → 掷币台出现 → 点"抛掷"（第1爻）… 连点六次 → 自动揭晓卦象 + Kimi 解读。

## 阴阳约定（和之前一致）
铜钱字面（乾隆通宝那面）= 阳；每爻抛三枚，字面记2、背面记3，和为6/7/8/9 → 老阴/少阳/少阴/老阳。
动画落定后字面朝上的枚数，与最终那一爻一一对应，视觉与卦象一致。

## 若图片不显示
确认 `public/assets/` 下有 coin-front.png 和 coin-back.png 两个文件即可。
